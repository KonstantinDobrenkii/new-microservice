# Template for local.properties
# Steps:
# Create /resources/local.properties file
# Copy the below content to /resources/local.properties
# Restart the spring application
spring.profiles.active=wiremock
# database properties
db.host=localhost:3306
db.name=iwms
db.user=iwms
db.password=iwms
# liquibase properties
# must contain brand/banner name, e.g.: dll ab alb mi ms
spring.liquibase.contexts=local,mi
# oauth2 properties
oauth2.auth.server=https://localhost
oauth2.client.secret=2Ha*uFqY
iwms.service.oauth2.remote.auth.enabled=false
# should be true ONLY for local environment
hostname.verification.disabled=true
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
